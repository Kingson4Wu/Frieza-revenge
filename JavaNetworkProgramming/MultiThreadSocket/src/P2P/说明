大概分析了代码之后，其实原理和fileTransfer包的基本一样
只不过加了客户端图形界面以及细节上的完善

-------------------------------------------

这是一个客户端程序
对方也要一个这样的客户端程序
就可以进行相互传输了

若只在本机测试，可把发送的ip地址设为localhost，即自己发送自己接受

本程序采用TCP和多线程传输文件

MainFrame是主界面
ApplicationStart启动客户端
Server是客户端服务程序


--------程序看了大概，并未全部看完----------------
